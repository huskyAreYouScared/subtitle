const state = {
  videoInfo:{
    duration:0
  }
}

const mutations = {
  setVideoInfo (state, data) {
    state.videoInfo = data
  }
}

const actions = {

}

export default {
  state,
  mutations,
  actions
}